import { FC } from 'react';
import { CombinationContent } from '@features/combination-content';

export const Combination: FC = () => {

  return (
    <div className="container">
      <CombinationContent />        
    </div>
  )
}